{{ define "response" }}[
{{- $lastPage := sub (len (where .Data.Pages "Section" "docs")) 1 }}
{{- range $index, $e := where .Data.Pages "Section" "docs" }}
  {{ (replace (chomp (.Render "item")) "\n" "\n  ") }}
  {{- if lt $index $lastPage }},{{ end }}
{{- end }}
]{{ end }}
